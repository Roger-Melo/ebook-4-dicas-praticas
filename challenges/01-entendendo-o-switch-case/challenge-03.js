/*
Escreva um switch/case que dê match com o valor da propriedade
"programming" do objeto stack, na próxima página.

As possibilidades a serem cobertas por este match são 'Clojure',
'JavaScript' e 'Python'.

As mensagens a serem exibidas devem ser:

- Clojure: 'Lisp's dialect.'
- JavaScript: 'Often abbreviated as JS.'
- Python: 'General-purpose programming language.'

Não modifique o código que já existe. Apenas siga o que os comentários
pedem.
*/

const stack = {
  markup: 'HTML',
  style: 'CSS',
  programming: 'JavaScript',
  versioning: 'Git'
}

const getProgrammingLanguage = (language) => {
  // Escreva o switch/case aqui
}

// Exiba a invocação de getProgrammingLanguage no console

stack.programming = 'Python'
// Faça o mesmo aqui

stack.programming = 'Clojure'
// E aqui também